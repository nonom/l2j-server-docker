# Build example from sibling datapack repository.

services:
  l2j-server-game:
    build:
      context: ../l2j-server-game-docker
      dockerfile: ../l2j-server-docker/build/Dockerfile.game
      additional_contexts:
        local-datapack: ../l2j-server-datapack
    image: l2j-server-game:local
