## l2j-server-docker build

Place this folder in `server/` to build local Docker images from sibling repositories.

Linux:
```bash
make build
make rebuild
```

Windows:
```bat
compose build
compose rebuild
```
