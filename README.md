# l2j-server-easy-stack

Quick start:

```bash
cp .env.example .env
docker compose up -d
docker compose down
docker compose ps
docker compose logs -f
```

Make commands:

```bash
make up
make down
make logs
make ps
make custom
```
