# l2j-server-easy-stack

## Quick start

Make commands:

```bash
make up
make down
make logs
make ps
make recreate
make restart
make custom
```

Windows commands:

```bat
compose up
compose down
compose logs
compose ps
compose recreate
compose restart
compose custom
```
